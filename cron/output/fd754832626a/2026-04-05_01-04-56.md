# Cron Job: Hermes git backup

**Job ID:** fd754832626a
**Run Time:** 2026-04-05 01:04:56
**Schedule:** every 10m

## Prompt

[SYSTEM: If you have a meaningful status report or findings, send them — that is the whole point of this job. Only respond with exactly "[SILENT]" (nothing else) when there is genuinely nothing new to report. [SILENT] suppresses delivery to the user. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Commit and push any changes in the ~/.hermes directory to GitHub. Steps:
1. cd ~/.hermes
2. Run `git status --porcelain` to check if there are changes
3. If there are changes:
   - Run `git diff` to see what actually changed
   - Analyze the diff and craft a DESCRIPTIVE commit message that explains WHAT changed and WHY it matters. Group related changes logically. Use conventional commits style when possible (e.g., "feat: add email monitoring skill", "fix: correct Zapier MCP indentation in config", "chore: update daily digest sources")
   - Run `git add -A`
   - Run `git commit -m "your descriptive message here"`
   - Run `git push origin master`
   - Summarize what was committed (list the key changes in your final response)
4. If no changes, report that everything is already committed and up to date

## Response

Committed and pushed **898c774** to `master`. Here's what changed:

### Key changes committed:

1. **`skills/email/check-relevant-emails/SKILL.md`** — Added a "Silence on Empty" directive: the email checker now stays silent if no relevant/new emails are found. Only reports back on actual findings or if the check itself fails (e.g., Gmail unreachable, timeout, MCP error).

2. **`skills/github/github-auth/SKILL.md`** — Added troubleshooting for SSH "Host key verification failed" errors with the fix: `ssh-keyscan github.com >> ~/.ssh/known_hosts`

3. **`cron/jobs.json`** — Registered the new **"Hermes git backup"** cron job, running every 10 minutes with conventional commit messaging so the git log doubles as a changelog.

4. **`config.yaml`** — Default model switched from `nemotron-3-super-120b-a12b:free` to `qwen/qwen3.6-plus:free`. Added blank `memory.provider` field.

5. **`.skills_prompt_snapshot.json`** — Deleted (1,600+ lines removed). This obsolete snapshot was superseded by the newer skills loading mechanism.

6. **`channel_directory.json`** — Timestamp update (routine).

7. **`hermes-agent`** — Submodule marked as dirty.
